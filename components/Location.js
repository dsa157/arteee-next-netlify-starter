import styles from './UI.module.css'

export default function Location({}) {
  return <button type="button"><img src="/map-pin.svg" alt="Generate from Location" className={styles.moduleButton} /></button>

}
