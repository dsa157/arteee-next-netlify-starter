import styles from './UI.module.css'

export default function Generate({}) {
  return <button type="button"><img src="/microphone.svg" alt="Generate from Prompt" className={styles.moduleButton} /></button>
}
